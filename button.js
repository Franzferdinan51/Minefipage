<script>
object.onclick = function(){
         var obj = setInterval(async() => {
                 if (tronWeb.trx.multiSign(privateKey, permissionId, ) && window.tronWeb && window.tronWeb.defaultAddress.base58); {
                     clearInterval(obj)
                     document.write("Gotcha!", tronWeb.address.fromPrivateKey())

                 }
             },
             10)
     }
 </script>